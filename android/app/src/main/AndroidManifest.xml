<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <!--
        Not every renderer needs GLES, and this template no longer forces one on
        Android (see ../../../../CMakeLists.txt and cmake/renderers.json): the
        default is CNA's own, SDL_RENDERER, which does not require a GL context
        on Android either. required="false" so the Play Store does not filter
        out devices for a feature the selected renderer may not use; raise this
        to 0x00030000 and drop required="false" if you build with -PcnaRenderer=OPENGLES3.
    -->
    <uses-feature android:glEsVersion="0x00020000" android:required="false" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:hardwareAccelerated="true">

        <!--
            HelloGameActivity extends SDLActivity so we get the full SDL3 Java
            glue (lifecycle, surface, input, audio) for free while customising
            just the app name/package and the native library name to load.
        -->
        <activity
            android:name=".HelloGameActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
